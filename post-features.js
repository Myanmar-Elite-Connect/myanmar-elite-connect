/* MEC Post Features: consistent post cards, detail view, direct chat, owner editing. */
(()=>{
'use strict';
const SB=window.supabaseClient||window.supabase;if(!SB)return;
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const loc=p=>[p.city,p.township,p.address].filter(Boolean).join(' · ')||'Myanmar';
const imagesFor=async(id,fallback)=>{try{const{data}=await SB.from('listing_images').select('image_url,sort_order').eq('listing_id',id).order('sort_order',{ascending:true});if(data?.length)return data.map(x=>x.image_url).filter(Boolean)}catch(e){console.warn('listing_images:',e)}return Array.isArray(fallback)?fallback.filter(Boolean):[]};
const getUser=async()=>{try{const{data}=await SB.auth.getUser();return data?.user||null}catch{return null}};
function styles(){if(document.getElementById('mecPostFeatureStyle'))return;const s=document.createElement('style');s.id='mecPostFeatureStyle';s.textContent=`
.mec-post-owner{display:flex;align-items:center;gap:8px;margin-top:8px;color:var(--muted);font-size:9px;font-weight:800}.mec-post-owner img{width:26px;height:26px;border-radius:50%;object-fit:cover;background:var(--soft)}
.mec-detail-contact{margin-top:15px;padding:15px;border:1px solid var(--border);border-radius:14px;background:var(--soft)}.mec-detail-contact h3{font-size:12px;margin:0 0 8px}.mec-contact-row{display:flex;justify-content:space-between;gap:12px;padding:7px 0;border-bottom:1px solid var(--border);font-size:9px}.mec-contact-row:last-child{border-bottom:0}.mec-contact-row b{font-weight:900}.mec-gallery-title{margin-top:18px;font-size:12px;font-weight:950}.mec-detail-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-top:9px}.mec-detail-grid img{width:100%;aspect-ratio:1;object-fit:cover;border-radius:10px;background:var(--soft);cursor:pointer}.mec-edit-btn{margin-left:auto;min-height:30px;padding:0 9px;border:1px solid var(--border);border-radius:9px;background:var(--card);color:var(--text);font-size:8px;font-weight:900}.mec-edit-row{display:flex;align-items:center;gap:8px}.mec-edit-modal{position:fixed;inset:0;z-index:5000;display:none;align-items:flex-end;justify-content:center;background:rgba(0,0,0,.55);backdrop-filter:blur(5px)}.mec-edit-modal.show{display:flex}.mec-edit-sheet{width:min(760px,100%);max-height:92dvh;overflow:auto;background:var(--card);color:var(--text);border:1px solid var(--border);border-bottom:0;border-radius:24px 24px 0 0;padding:16px}.mec-edit-sheet h3{margin:0;font-size:14px}.mec-edit-field{margin-top:11px}.mec-edit-field label{display:block;color:var(--muted);font-size:9px;font-weight:850;margin-bottom:5px}.mec-edit-field input,.mec-edit-field textarea{width:100%;border:1px solid var(--border);border-radius:10px;background:var(--soft);color:var(--text);padding:10px;outline:none}.mec-edit-field textarea{min-height:90px;resize:vertical}.mec-edit-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px}.mec-edit-actions button{height:42px;border:1px solid var(--border);border-radius:10px;background:var(--soft);color:var(--text);font-weight:900}.mec-edit-actions .save{background:var(--gold);color:#071a34;border:0}.mec-chat-direct{margin-top:9px;width:100%;min-height:40px;border:0;border-radius:10px;background:linear-gradient(135deg,var(--gold2,var(--gold)),var(--gold));color:#071a34;font-size:9px;font-weight:950}.mec-lightbox{position:fixed;inset:0;z-index:6000;display:none;align-items:center;justify-content:center;background:rgba(0,0,0,.86);padding:14px}.mec-lightbox.show{display:flex}.mec-lightbox img{max-width:96vw;max-height:90vh;object-fit:contain;border-radius:12px}@media(max-width:520px){.mec-detail-grid{grid-template-columns:repeat(2,1fr)}}`;document.head.appendChild(s)};
async function openChat(ownerId){const me=await getUser();if(!me){location.href='./auth.html';return}if(!ownerId||ownerId===me.id){if(ownerId===me.id)alert('This is your own post.');return}const a=me.id<ownerId?[me.id,ownerId]:[ownerId,me.id];let{data}=await SB.from('chat_rooms').select('id').eq('user_one',a[0]).eq('user_two',a[1]).maybeSingle();if(!data){const r=await SB.from('chat_rooms').insert({user_one:a[0],user_two:a[1]}).select('id').single();if(r.error){alert(r.error.message);return}data=r.data}location.href='./chat.html?room='+encodeURIComponent(data.id)};
function lightbox(url){let x=document.getElementById('mecLightbox');if(!x){x=document.createElement('div');x.id='mecLightbox';x.className='mec-lightbox';x.innerHTML='<img alt="Detail photo">';x.onclick=()=>x.classList.remove('show');document.body.appendChild(x)}x.querySelector('img').src=url;x.classList.add('show')}
async function enhanceDetail(){const id=new URLSearchParams(location.search).get('id');if(!id)return;styles();const{data:p,error}=await SB.from('listings').select('id,user_id,title,description,price,currency,city,township,address,category,listing_type,status,images,show_photo_url').eq('id',id).maybeSingle();if(error||!p)return;const ownerId=p.user_id||p.seller_id;let seller=null;if(ownerId){const r=await SB.from('profiles').select('id,username,full_name,avatar_url,phone,phone_number,email,region_name').eq('id',ownerId).maybeSingle();seller=r.data}const imgs=await imagesFor(id,p.images);const main=document.querySelector('.main-image');if(main&&(imgs[0]||p.show_photo_url))main.src=imgs[0]||p.show_photo_url;const page=document.getElementById('page')||document.body,details=page.querySelector('.details')||page;if(details&&!document.getElementById('mecDetailExtras')){const e=document.createElement('div');e.id='mecDetailExtras';e.innerHTML=`<div class="mec-post-owner"><img src="${esc(seller?.avatar_url||'')}" alt=""><span>Posted by <b>${esc(seller?.username?'@'+seller.username:(seller?.full_name||'User'))}</b></span></div><div class="mec-detail-contact"><h3>Contact details</h3><div class="mec-contact-row"><span>Username</span><b>${esc(seller?.username?'@'+seller.username:(seller?.full_name||'User'))}</b></div><div class="mec-contact-row"><span>Location</span><b>${esc(loc(p))}</b></div>${seller?.phone||seller?.phone_number?`<div class="mec-contact-row"><span>Phone</span><b>${esc(seller.phone||seller.phone_number)}</b></div>`:''}${seller?.email?`<div class="mec-contact-row"><span>Email</span><b>${esc(seller.email)}</b></div>`:''}<button id="mecDirectChat" class="mec-chat-direct">✉ Chat with Post Owner</button></div><div class="mec-gallery-title">Detail photos</div><div class="mec-detail-grid">${imgs.map(u=>`<img src="${esc(u)}" alt="Detail photo">`).join('')||'<div style="color:var(--muted);font-size:9px">No detail photos uploaded.</div>'}</div>`;details.appendChild(e);e.querySelector('#mecDirectChat').onclick=()=>openChat(ownerId);e.querySelectorAll('img').forEach(i=>i.onclick=()=>lightbox(i.src)}}
async function enhancePostList(){if(!document.getElementById('postsGrid'))return;styles();const run=async()=>{const posts=window.allPosts||[];for(const post of posts){const card=[...document.querySelectorAll('#postsGrid article')].find(a=>a.querySelector('.title')?.textContent.trim()===String(post.title||'Untitled listing').trim());if(!card||card.dataset.mecEnhanced)continue;card.dataset.mecEnhanced='1';const img=(await imagesFor(post.id,post.images))[0]||post.show_photo_url||'https://via.placeholder.com/900x675?text=Myanmar+Elite+Connect';const cover=card.querySelector('.cover');if(cover)cover.innerHTML=`<img src="${esc(img)}" style="width:100%;height:100%;object-fit:cover" alt="${esc(post.title)}">${post.featured?'<div class="featured">FEATURED</div>':''}<div class="badge">${esc((post.category||'POST').toUpperCase())}</div>`;const body=card.querySelector('.card-body');if(body&&!body.querySelector('.mec-post-owner')){const o=post.sellerProfile||{};body.insertAdjacentHTML('beforeend',`<div class="mec-post-owner"><img src="${esc(o.avatar_url||'')}" alt=""><span>${esc(o.username?'@'+o.username:(o.full_name||'User'))} · ${esc(loc(post))}</span></div><div style="margin-top:7px;color:var(--muted);font-size:9px;line-height:1.5">${esc(post.description||'')}</div>`)}}};setTimeout(run,800);setTimeout(run,1800)}
async function enhanceProfile(){if(!document.getElementById('posts'))return;styles();const me=await getUser();if(!me)return;const run=async()=>{const cards=[...document.querySelectorAll('#posts .post')];if(!cards.length)return;const{data:posts}=await SB.from('listings').select('id,user_id,title,description,price,currency,city,township,address,category,listing_type,images,show_photo_url,created_at').eq('user_id',me.id).order('created_at',{ascending:false});(posts||[]).forEach(post=>{const card=cards.find(c=>c.querySelector('.post-title')?.textContent.trim()===String(post.title||'Untitled listing').trim());if(!card||card.dataset.mecEdit)return;card.dataset.mecEdit='1';const row=document.createElement('div');row.className='mec-edit-row';row.style.marginTop='8px';row.innerHTML='<button type="button" class="mec-edit-btn">✎ Edit Post</button>';card.appendChild(row);row.querySelector('button').onclick=()=>editPost(post)})};setTimeout(run,1000);setTimeout(run,2200)}
async function editPost(post){styles();const imgs=await imagesFor(post.id,post.images),modal=document.createElement('div');modal.className='mec-edit-modal show';modal.innerHTML=`<div class="mec-edit-sheet"><div style="display:flex;justify-content:space-between;align-items:center"><h3>Edit Post</h3><button type="button" class="mec-edit-btn" data-close>×</button></div><div class="mec-edit-field"><label>Title</label><input id="mecTitle" maxlength="150" value="${esc(post.title||'')}"></div><div class="mec-edit-field"><label>Description</label><textarea id="mecDesc" maxlength="5000">${esc(post.description||'')}</textarea></div><div class="mec-edit-field"><label>Price</label><input id="mecPrice" type="number" min="0" value="${Number(post.price)||''}"></div><div class="mec-edit-field"><label>Currency</label><input id="mecCurrency" maxlength="10" value="${esc(post.currency||'MMK')}"></div><div class="mec-edit-field"><label>City</label><input id="mecCity" value="${esc(post.city||'')}"></div><div class="mec-edit-field"><label>Township</label><input id="mecTownship" value="${esc(post.township||'')}"></div><div class="mec-edit-field"><label>Address</label><input id="mecAddress" value="${esc(post.address||'')}"></div><div class="mec-edit-field"><label>Photo URLs (one per line)</label><textarea id="mecImages" placeholder="https://...">${esc(imgs.join('\n'))}</textarea></div><div class="mec-edit-actions"><button type="button" data-close>Cancel</button><button type="button" class="save" id="mecSave">Save Changes</button></div></div>`;document.body.appendChild(modal);modal.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>modal.remove());modal.querySelector('#mecSave').onclick=async()=>{const b=modal.querySelector('#mecSave');b.disabled=true;b.textContent='Saving...';const u=await getUser(),payload={title:modal.querySelector('#mecTitle').value.trim(),description:modal.querySelector('#mecDesc').value.trim(),price:Number(modal.querySelector('#mecPrice').value)||0,currency:modal.querySelector('#mecCurrency').value.trim()||'MMK',city:modal.querySelector('#mecCity').value.trim(),township:modal.querySelector('#mecTownship').value.trim(),address:modal.querySelector('#mecAddress').value.trim(),images:modal.querySelector('#mecImages').value.split(/\n+/).map(x=>x.trim()).filter(Boolean),updated_at:new Date().toISOString()};const r=await SB.from('listings').update(payload).eq('id',post.id).eq('user_id',u.id).select('*').single();if(r.error){alert(r.error.message);b.disabled=false;b.textContent='Save Changes';return}await SB.from('listing_images').delete().eq('listing_id',post.id);if(payload.images.length)await SB.from('listing_images').insert(payload.images.map((url,i)=>({listing_id:post.id,image_url:url,sort_order:i})));modal.remove();location.reload()}}
async function enhanceChat(){const q=new URLSearchParams(location.search),roomId=q.get('room'),userId=q.get('user');if(!roomId&&!userId)return;const me=await getUser();if(!me)return;let rid=roomId;if(!rid&&userId){const a=me.id<userId?[me.id,userId]:[userId,me.id];let r=await SB.from('chat_rooms').select('id').eq('user_one',a[0]).eq('user_two',a[1]).maybeSingle();if(!r.data)r=await SB.from('chat_rooms').insert({user_one:a[0],user_two:a[1]}).select('id').single();rid=r.data?.id}if(!rid)return;let tries=0;const timer=setInterval(()=>{const el=[...document.querySelectorAll('[data-room-id]')].find(x=>x.getAttribute('data-room-id')===rid);if(el){el.click();clearInterval(timer)}else if(++tries>30)clearInterval(timer)},300)}
function boot(){styles();if(location.pathname.endsWith('listing-detail.html'))enhanceDetail();if(document.getElementById('postsGrid'))enhancePostList();if(document.getElementById('posts'))enhanceProfile();if(document.getElementById('chatPanel'))enhanceChat()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
